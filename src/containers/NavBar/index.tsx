import { FC } from "react"

const NavBar: FC = () => {
  const flexBetween = "flex items-center justify-between"
  return <div className={`${flexBetween} fixed top-0 z-30 w-full`}>index</div>
}

export default NavBar
